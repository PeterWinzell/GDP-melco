#include <gtest/gtest.h>
#include <gmock/gmock-matchers.h>

using namespace testing;

TEST(W3CServerClass, methodName)
{

//    EXPECT_EQ(str, "hello test");
//    ASSERT_THAT(10, Eq(str.length()));
}
